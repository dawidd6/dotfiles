#!/usr/bin/env bash

set -euo pipefail

clipboard="$(wl-paste --type text 2>/dev/null)"
buffer="$(tmux show-buffer 2>/dev/null)"

if [[ "$clipboard" != "$buffer" ]]; then
    printf "%s" "$clipboard" | tmux load-buffer -
fi

tmux choose-buffer -Z
