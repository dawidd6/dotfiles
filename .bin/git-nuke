#!/usr/bin/env bash

set -euo pipefail

read -rp $'\e[96m\e[1m==> \e[39mClean? [y/N]\e[0m ' CLEAN

if [[ "$CLEAN" =~ ^[Yy]$ ]]; then
    root="$(git rev-parse --show-cdup)"
    root="${root:-.}"
    git clean -fd
    git checkout -- "${root}"
fi
