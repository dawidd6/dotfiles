set hidden true
set icons true
set promptfmt "\033[32;1m%d\033[0m\033[1m%f\033[0m"

cmd quit-print ${{
    if [ "$lf_flag_print_selection" = "true" ]; then
        printf '%s\n' "$fx" > "/proc/$id/fd/1"
    fi
    lf -remote "send $id quit"
}}

map <enter> quit-print
